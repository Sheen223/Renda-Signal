"use client";

import { useSearchParams } from "next/navigation";
import { SiteHeader } from "../components/site-header";

const networks=[{name:"X",mark:"𝕏",copy:"Send funded requests to any verified X account.",live:true},{name:"GitHub",mark:"GH",copy:"Reward maintainers, reviewers and contributors.",live:false},{name:"Facebook",mark:"f",copy:"Reach creators, communities and local businesses.",live:false},{name:"LinkedIn",mark:"in",copy:"Make serious, funded professional introductions.",live:false},{name:"Discord",mark:"◖",copy:"Direct requests to community members and moderators.",live:false},{name:"Telegram",mark:"✈",copy:"Fund private requests without collecting wallets.",live:false}];
export default function ExplorePage(){const params=useSearchParams();const error=params.get("error");return <main className="explore-page"><SiteHeader light/><section className="explore-intro"><span className="eyebrow">CHOOSE A CONVERSATION LAYER</span><h1>Where do you want<br />to send a signal?</h1><p>Connect the social identity you already use. Renda verifies who a request is for; Nimiq Pay handles the money.</p>{error&&<div className="auth-error">{error==="x_not_configured"?"X OAuth is ready in the app, but the X Client ID and session secret must be added to hosting.":"X sign-in did not complete. Please try again."}</div>}</section><section className="network-grid">{networks.map(n=>n.live?<a key={n.name} href="/api/auth/x/start?returnTo=/x" className="network-card live"><div className="network-top"><span className="network-mark">{n.mark}</span><span className="live-badge">LIVE</span></div><h2>{n.name}</h2><p>{n.copy}</p><strong>Sign in with X <span>→</span></strong></a>:<article key={n.name} className="network-card disabled"><div className="network-top"><span className="network-mark">{n.mark}</span><span className="soon-badge">COMING SOON</span></div><h2>{n.name}</h2><p>{n.copy}</p><strong>Join waitlist</strong></article>)}</section><div className="explore-foot"><span>ONE IDENTITY</span><span>ONE WALLET PER REQUEST</span><span>PUBLIC SETTLEMENT</span></div></main>}

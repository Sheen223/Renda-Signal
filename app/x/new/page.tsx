"use client";

import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";

export default function NewSignalPage(){const router=useRouter();const[notice,setNotice]=useState("");function create(e:FormEvent<HTMLFormElement>){e.preventDefault();setNotice("Request prepared. Confirm the USDT approval in Nimiq Pay to fund it.");setTimeout(()=>router.push("/x/sent"),900)}return <section className="create-screen"><div className="create-copy"><span className="eyebrow">MAKE YOUR MESSAGE MATTER</span><h1>Send a<br/><em>Signal.</em></h1><p>Make a public, funded request to anyone on X. They earn for accepting your message and completing the agreed work.</p><ol><li><span>1</span>Target one verified X account</li><li><span>2</span>Lock Polygon USDT in escrow</li><li><span>3</span>Publish the funded X mention</li></ol></div><form className="signal-form" onSubmit={create}><div className="form-head"><span>NEW TARGETED REQUEST</span><b>Polygon · USDT</b></div><label>Who do you want to reach?<div className="x-input"><span>𝕏</span><input name="target" placeholder="@username" required/></div></label><label>What do you need?<input name="title" placeholder="Review my Nimiq onboarding" required maxLength={80}/></label><label>Clear terms<textarea name="brief" placeholder="Describe the exact deliverable and what counts as complete…" required rows={4}/></label><div className="two-fields"><label>Total reward<div className="money-input"><input name="amount" type="number" min="1" step="0.1" defaultValue="10"/><span>USDT</span></div></label><label>Attention fee<div className="money-input"><input name="attentionFee" type="number" min="0" step="0.1" defaultValue="1"/><span>USDT</span></div></label></div><label>Completion deadline<input name="due" type="datetime-local" required/></label><div className="escrow-note"><span>⌾</span><div><strong>Protected by two-stage escrow</strong><p>The attention fee is released on acceptance. The rest stays locked until approval, mutual settlement or arbitration.</p></div></div>{notice&&<p className="inline-notice">{notice}</p>}<button className="primary fund-button">Review & fund request <span>→</span></button></form></section>}
